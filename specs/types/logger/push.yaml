name: push
type: method
summary: Push a new logging context.
description: >-
  Create a new logging context and push it onto the logging stack.
receiver: Logger
signatures:
  - arguments:
    - name: name
      type: String
      description: The name of the logging context.
    returns:
      name: logger
      type: Logger
      description: The default logger.
