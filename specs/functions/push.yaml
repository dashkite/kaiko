name: push
type: function
summary: Creates a new logging context.
description: >-
  Create a new logging context and push it onto the logging stack using the default logger.
signatures:
  - arguments:
    - name: name
      type: String
      description: The name of the logging context.
    returns: &context
      name: context
      type: Context
      description: The logging context.
